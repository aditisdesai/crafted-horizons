// config.example.js — safe to commit, shows the required format.
// Copy this file to config.local.js (which is gitignored) and add your real keys.
window.AI_KEYS = {
  anthropic: '', // paste your Anthropic API key here (sk-ant-...)
  groq:      '', // paste your Groq API key here (gsk_...)
};
